package com.longnguyen.lensrecommender;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Lens Recommender Spring Boot application
 */
@SpringBootApplication
public class LensRecommenderApplication {

	public static void main(String[] args) {
		SpringApplication.run(LensRecommenderApplication.class, args);
	}
}